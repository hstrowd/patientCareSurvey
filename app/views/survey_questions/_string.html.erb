<div class="question <%= question.name %>">
  <%= label_tag(question.name, question.question) %>
  <div class="response">
    <%= text_field_tag(question.name, response.try(:response)) %>
  </div>
</div>
